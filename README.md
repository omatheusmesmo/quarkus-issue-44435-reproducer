# Quarkus Issue #44435 - Reproducer

Standalone reproducer for [Issue #44435](https://github.com/quarkusio/quarkus/issues/44435)

## Issue Context

GraalVM PR [#10030](https://github.com/oracle/graal/pull/10030) (merged Nov 2024) added detection of `System.getProperty("java.home")` calls during native image builds.

**Quarkus AWT Extension** has a workaround that creates a fake `java.home` directory:
- File: `extensions/awt/runtime/src/main/java/io/quarkus/awt/runtime/JDKSubstitutions.java`
- Substitutes: `sun.awt.FontConfiguration.setOsNameAndVersion()`
- Creates: `/tmp/quarkus-awt-tmp-fonts` directory structure

## What This Reproducer Tests

### Test 1: AWT Font Loading (Standard Behavior)
Endpoint: `/fonts`
- Triggers AWT font initialization
- Uses Quarkus AWT extension
- GraalVM shows generic HOME recommendation (no specific warnings)
- Workaround functions correctly

### Test 2: Explicit java.home Access
Endpoint: `/javahome`
- Explicitly calls `System.getProperty("java.home")`
- GraalVM shows generic HOME recommendation (no specific warnings)
- Same behavior as Test 1 - validates consistent detection

## Running the Tests

### Automated Testing (Recommended)
```bash
# Run complete test suite with automated log generation
./test-issue-44435.sh

# This will generate:
# - logs/test-TIMESTAMP.log (full test log)
# - logs/summary-TIMESTAMP.txt (executive summary)
```

### Manual Testing
```bash
# Build native image
./mvnw clean package -Dnative -DskipTests

# Run the application
./target/*-runner &

# Test AWT endpoint
curl http://localhost:8080/fonts

# Test explicit endpoint
curl http://localhost:8080/javahome
```

## What to Look For

### During Build (All Scenarios)
```
[2/8] Performing analysis...
# No specific warnings about java.home call locations

Recommendations:
...
HOME: To avoid errors, provide java.home to the app with '-Djava.home=<path>'.
...
```

GraalVM PR #10030 shows generic HOME recommendation for both:
- AWT substitution code
- Explicit application code

Same output, no distinction between code types.

### Runtime Behavior
```
# /fonts endpoint (AWT)
java.home: null
Fonts loaded: 2474
Font initialization: SUCCESS

# /javahome endpoint (explicit)
java.home: /tmp/quarkus-awt-tmp-fonts
# Property is set by Quarkus substitution
```

## Evidence Files

Generated by `test-issue-44435.sh`:
- `logs/test-TIMESTAMP.log` - Complete build and test logs
- `logs/summary-TIMESTAMP.txt` - Executive summary with key metrics

## Project Structure

```
src/main/java/org/acme/
├── FontResource.java        # AWT font test endpoint
├── JavaHomeResource.java    # Explicit java.home test endpoint
└── GreetingResource.java    # Basic hello endpoint

logs/
├── test-TIMESTAMP.log       # Automated test logs
└── summary-TIMESTAMP.txt    # Test summaries

test-issue-44435.sh          # Automated test script
```

## Versions

- Quarkus: 3.34.2
- GraalVM: 25.0.2 (Oracle)
- Java: 25.0.2

## Conclusions

1. **GraalVM PR #10030 behavior**: Shows generic HOME recommendation without specific call locations
2. **No code distinction**: Same output for AWT substitution and explicit application code
3. **Runtime behavior correct**: java.home is properly set to `/tmp/quarkus-awt-tmp-fonts` by Quarkus substitution
4. **Integration tests pass**: All tests pass, confirming workaround functionality
5. **Workaround is effective**: Quarkus AWT workaround prevents specific detection

This reproducer demonstrates that GraalVM PR #10030 provides generic recommendations without distinguishing between substitution code and application code. The current Quarkus workaround is sufficient.

## See Also

- Issue: https://github.com/quarkusio/quarkus/issues/44435
- GraalVM PR: https://github.com/oracle/graal/pull/10030
- Quarkus Workaround: `extensions/awt/runtime/src/main/java/io/quarkus/awt/runtime/JDKSubstitutions.java`
